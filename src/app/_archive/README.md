# Archive

This folder is excluded from the Next.js route table (underscore prefix) and from the TypeScript compiler (see `tsconfig.json` exclude list).

Contents are kept only as source material for the rebuild against the Cognifica rebuild brief. Do not import from here. Do not copy copy blocks verbatim. Every page in the new architecture is rewritten from the brief against the Section 2 writing rules.

## Files

- `old-home.tsx` — the pre-rebuild single-page home. Its content feeds `/for-employers` (rewritten, not lifted).
